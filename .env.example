# Environment variables for development and production

# Required for the API server
PORT=3000
NODE_ENV=development

# Required for database connection (PostgreSQL)
# Example: postgresql://user:password@localhost:5432/daphnes_root
DATABASE_URL=

# Optional: Set to true in production to enable optimizations
PRODUCTION=false
